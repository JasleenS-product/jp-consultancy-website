---
layout: null
permalink: "/index.html"
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Future-Ready Hospital Operations | JP Consultancy</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      min-height: 100vh;
      min-width: 100vw;
      background: linear-gradient(135deg, #0a0f1c 0%, #1f2a44 100%);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      font-family: 'IBM Plex Sans', sans-serif;
      transition: opacity 0.8s cubic-bezier(0.4,0,0.2,1);
      opacity: 1;
      animation: slideUpFromBottom 2s cubic-bezier(0.4,0,0.2,1);
      position: relative;
      overflow: hidden;
    }
    body::before {
      content: '';
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      z-index: 0;
      background: rgba(20,24,40,0.45);
      backdrop-filter: blur(24px) saturate(160%);
      -webkit-backdrop-filter: blur(24px) saturate(160%);
      pointer-events: none;
    }
    .splash-logo {
      width: 600px;
      height: auto;
      margin-bottom: 2rem;
      filter: drop-shadow(0 0 24px #fff) drop-shadow(0 0 32px #1f5fff88);
      display: block;
      object-fit: contain;
      z-index: 1;
    }
    .splash-message {
      font-size: 1.4rem;
      font-weight: 500;
      letter-spacing: 0.02em;
      color: #fff;
      text-shadow: 0 2px 12px rgba(31,95,255,0.12);
      text-align: center;
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      line-height: 1.4;
      max-width: 800px;
      margin: 0 auto;
      z-index: 1;
    }
    @keyframes slideUpFromBottom {
      0% { transform: translateY(100vh); }
      100% { transform: translateY(0); }
    }
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(60px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes logoPop {
      0% { opacity: 0; transform: translateY(60px); }
      100% { opacity: 1; transform: translateY(0); }
    }
    .fade-out {
      opacity: 0 !important;
      pointer-events: none;
      transition: opacity 0.7s cubic-bezier(0.4,0,0.2,1);
    }
    .loader-overlay {
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background: linear-gradient(135deg, #0a0f1c 0%, #1f2a44 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 1000;
      transition: opacity 0.5s;
    }
    .loader-overlay.hide {
      opacity: 0;
      pointer-events: none;
    }
    .spinner {
      width: 64px;
      height: 64px;
      border: 6px solid #fff3;
      border-top: 6px solid #fff;
      border-radius: 50%;
      animation: spin 1s linear infinite;
    }
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    @media (max-width: 600px) {
      .splash-logo { width: 320px; height: auto; margin-bottom: 1.2rem; }
      .splash-message { font-size: 1.1rem; max-width: 90vw; }
    }
  </style>
</head>
<body>
  <div class="loader-overlay" id="loader">
    <div class="spinner"></div>
  </div>
  <div id="splash-content" style="opacity:0; pointer-events:none; transition: none; display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 100vh;">
    <img src="/assets/images/JP CONSULTANCY WHITE SVG.svg" alt="JP Consultancy Logo" class="splash-logo" />
    <div class="splash-message">Building the Next Generation of Hospitals—Smarter, Safer, Future-Ready.</div>
  </div>
  <script>
    setTimeout(function() {
      document.getElementById('loader').classList.add('hide');
      var splash = document.getElementById('splash-content');
      splash.style.opacity = 1;
      splash.style.pointerEvents = 'auto';
      splash.style.transition = 'none';
    }, 1750);
    function goHome() {
      document.body.classList.add('fade-out');
      setTimeout(function() {
        window.location.href = "/home";
      }, 700);
    }
    setTimeout(goHome, 4750);
    document.body.addEventListener('click', goHome);
    document.body.addEventListener('touchstart', goHome);
  </script>
</body>
</html> 